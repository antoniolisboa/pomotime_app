import 'package:flutter/material.dart';

class PomodoroTimerSelectorDropdown extends StatelessWidget {
  const PomodoroTimerSelectorDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
